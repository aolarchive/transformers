{
    "name": "aol/transformers",
    "description": "A package for normalizing property names and types from an external data store.",
    "keywords": ["transformation", "layer", "normalizing", "data store", "mysql", "mongo"],
    "homepage": "http://github.com/aol/transformers",
    "license": "MIT",
    "require": {
        "php": ">=5.4.0"
    },
    "require-dev": {
        "phpunit/phpunit": "~4.1"
    },
    "autoload": {
        "psr-4": {
            "Aol\\Transformers\\": "lib/"
        }
    },
    "extra": {
        "aura": {
            "type": "project",
            "config": {
                "common": "Aol\\Transformers\\Config"
            }
        }
    }
}
